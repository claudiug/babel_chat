-> /login with get
-> /login with post
-> on post and valid data redirect to messages#index
-> on messages#index we have a remore form that post to messages#create
-> on message#create we grab the lanaguage type
-> maybe sidekik??? and move on another job
-> make classes for each language endpoint???
-> use nokogiri to parse the result and json to parse the json response
-> use httparty to post to faye channel
-> list of users??????

rackup faye.ru -E production -s puma
